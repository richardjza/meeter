{
  "name": "meeter",
  "version": "0.1.0",
  "private": true,
  "description": "A simple, lightweight way to plan meetings across timezones.",
  "license": "Apache-2.0",
  "scripts": {
    "test": "node tests/app.test.mjs",
    "start": "node -e \"import('./tests/server.mjs').then(async m => { const s = await m.startServer('.'); console.log('Meeter running at ' + s.url); })\""
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
